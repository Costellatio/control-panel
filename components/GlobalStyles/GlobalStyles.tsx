function GlobalStyles() {
  return (
    <style>{`

      html,
      body {
        height: 100%;
      }

      body {
        background: #111;
      }

    `}</style>
  );
}

export default GlobalStyles;
